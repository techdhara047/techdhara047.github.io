

   
    <form action="">
        <div class="form-group"><input type="text" placeholder="Name"><input type="text" placeholder="Phone Number"></div>
        <div class="form-group"><input type="text" placeholder="Email"></div>
        <div class="form-group"><input type="text" placeholder="Subject"></div>
        <div class="form-group"><textarea placeholder="write your message here..."></textarea></div>
        <div class="form-group"><button class="btn">Get in Touch</button></div>
    </form>


<style>
 
   .form-group {
    margin-block: 1rem;
    display: flex;
    
   }
   .form-group:first-child {
    display: flex;
    gap: 2rem;
   }
   .form-group:first-child input{
    flex: 1;
   }
   input {
   font-size: 1.4rem;
    width: 100%;
    padding: 0.4rem;
    border: none;
    background-color: inherit;
    border-bottom: 1px solid black;
    outline: none;
    color: var( --text-color);
    
   }
   textarea { font-size: 1.4rem;
    width: 100%;
    height: 14rem;
    background-color: var( --card-color);
   }
   .btn {
    width: 100%;
    cursor: pointer;
   }
   input::placeholder, 
        textarea::placeholder {
            color: var(--text-color); /* You can set this to any color you want */
            opacity: 1; /* Ensure full opacity for consistent color across browsers */
        }
 
</style>

